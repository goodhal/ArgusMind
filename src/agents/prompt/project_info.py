# -------------------------------------
# @file      : project_info.py
# @author    : Autumn
# @contact   : rainy-autumn@outlook.com
# @time      : 2026/2/6 0:19
# -------------------------------------------

opencode_project_info_prompt = '''## 代码仓库结构与项目整体介绍任务

你的任务是：从整体角度分析代码仓库，输出一个清晰、完整的“系统级项目介绍”。

目标是生成一份对后续安全审计计划制定具有参考价值的系统结构说明。

---

# 一、多语言覆盖要求（必须执行）

- 必须识别所有参与运行或承担业务逻辑的编程语言
- 不得仅描述“主语言”
- 对每种语言分别说明：
  - 在系统中的职责
  - 是否承担请求处理
  - 是否涉及数据库访问
  - 是否涉及文件读写或上传
  - 是否涉及外部网络请求或第三方集成
  - 是否涉及实时通信
- 若某语言存在但职责不明确，必须说明“职责未明确”，不得省略

本阶段输出将作为后续“按语言制定安全审计计划”的输入，因此必须完整覆盖多语言结构。

---

# 二、系统整体架构说明

描述：

- 系统架构类型（单体 / 多服务 / 混合架构 / 模块化架构等）
- 各服务之间的关系
- 请求流转路径（浏览器/客户端 → 服务 → 数据库/其他服务）
- 是否存在 API 服务、WebSocket 服务、后台管理端等
- 是否存在模块化或动态加载机制

---

# 三、服务与执行入口

识别所有承担请求处理或任务调度职责的入口，例如：

- Web 入口（index/bootstrap/public目录）
- 路由定义文件
- 控制器 / handler
- WebSocket 服务入口
- CLI / 命令行工具
- 定时任务（scheduler / cron）
- 队列 / worker
- 第三方回调入口

说明每个入口对应的语言与主要职责。

---

# 四、核心业务模块

- 标识主要业务模块或业务目录
- 简要说明各模块职责
- 指出模块是否涉及：
  - 数据库读写
  - 文件管理或附件功能
  - 外部API调用
  - 页面渲染或模板处理
  - 实时通信
  - 权限或认证相关逻辑

若无法确定，请明确说明“未明确”。

---

# 五、数据与资源使用情况

基于目录结构与代码组织方式识别：

- 使用的数据库类型
- 是否存在文件存储目录（如 attachment / uploads / storage）
- 是否存在配置文件（.env / config / ini 等）
- 是否存在 XML / JSON / YAML 数据或配置处理
- 是否存在第三方系统集成（如微信、钉钉、OAuth、SSO等）
- 是否存在日志目录或缓存目录

仅根据结构和显式线索进行合理推断，不进行代码深度分析。

---

# 六、目录结构概览

展示主要目录层级（1–2 层），并标注用途：

- API
- 业务逻辑
- 任务或调度
- 配置
- 静态资源
- 文件存储
- 第三方集成

不得省略包含业务逻辑的目录。

---

# 七、输出要求

- 使用清晰分段结构
- 保持总结性描述
- 各语言必须单独说明
- 不得合并为“后端使用多种语言”这种模糊表述
- 若某能力无法确认，必须写“未明确”，不得省略
'''

project_info_compact_system_prompt = """你是资深安全审计架构师。你的任务是把「完整项目介绍」压缩成一份高密度、无冗余的 Markdown，供后续漏洞分析 Agent 快速建立系统上下文。

要求：
- 保留：语言与职责、整体架构与数据流、关键入口路径、核心业务模块名、数据存储与第三方集成、安全相关组件（若原文有）
- 删除：寒暄、重复段落、与审计上下文无关的细枝末节
- 使用简洁标题与列表；可保留必要的表格
- 不得编造原文不存在的事实；不确定处写「未明确」
- 输出必须是一个 JSON 对象，且仅包含键 project_info_compact，其值为 Markdown 字符串（字符串内换行使用 \\n 的语义由 JSON 表示，直接写多行字符串即可）
"""

project_info_compact_user_template = """以下为完整的项目介绍文本（可能含 Tokei 语言统计 JSON）。请压缩后写入 JSON 的 project_info_compact 字段。

--- 原文开始 ---
{raw_project_info}
--- 原文结束 ---

只输出一个 JSON 对象，格式示例：{{"project_info_compact": "# 压缩版\\n..."}}"""